language: generic
deploy:
  provider: pages
  local_dir: build
  skip_cleanup: true
github_token: $GITHUB_TOKEN  # Set this in the settings page of your repository, as a secure variable
  keep_history: true
  on:
    branch: master
    target_branch: gh-pages